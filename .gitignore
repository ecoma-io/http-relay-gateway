bin/
.claude/
node_modules/

# Runtime state lives here once the SQLite store exists; never committed.
data/
*.db
*.db-shm
*.db-wal

# The SPA build output is generated by `make web`; only the placeholder stub
# is tracked, so plain `go build`/`go test` work without a node toolchain.
internal/web/dist/*
!internal/web/dist/index.html

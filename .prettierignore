# Semgrep fixtures are load-bearing bytes: their whole job is to match (or
# not match) rules, and prettier reformatting them would invalidate
# `semgrep --test` in analysis.yml.
.github/semgrep/
bin/
pnpm-lock.yaml
node_modules/

# Build output is generated (the committed stub under internal/web/dist must
# survive untouched — it is the placeholder that lets `go build` work without
# a node toolchain).
web/dist/
internal/web/dist/

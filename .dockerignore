# The runtime config is bind-mounted at runtime and must not enter builds.
config.yaml

# Runtime state (SQLite DB, WAL files) must never enter the image.
data

# Local development artifacts do not belong in the build context.
.git
.claude
bin
e2e

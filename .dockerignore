# Runtime state (SQLite DB, WAL files) must never enter the image.
data

# The UI bundle is built inside the image; a stale host-side copy must not
# leak into the build (the committed stub exists for host `go build` only).
web/dist
internal/web/dist

# Local development artifacts do not belong in the build context.
.git
.claude
bin
e2e
